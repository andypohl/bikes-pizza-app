<?xml version="1.0" encoding="utf-8"?>
<!-- Launch screen on Android 11 and older (Android 12+ draws the system
     splash screen configured in values-v31/styles.xml instead): the logo
     and wordmark, centered on the icon's teal. The artwork is generated by
     tool/splash/render_splash.py. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash" />
    </item>
</layer-list>
