# Launch Screen Assets

The images here are generated by `tool/splash/render_splash.py` (the
logo and wordmark that LaunchScreen.storyboard centers on the teal
background); rerun that script rather than editing them by hand.
